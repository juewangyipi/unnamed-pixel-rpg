# 「待命名」是怎么做出来的？—— 通俗版说明书

> 写给：只有一点 Python 基础、对前端/游戏开发还不熟的同学。  
> 目标：用大白话讲清楚——**这游戏是什么、用了什么工具、代码怎么分的、为什么这么做**。

---

## 0. 先用一句话说清楚

这是一个 **在浏览器里玩的、俯视角像素小游戏**（第一版 v0.1）：

- 你控制一个小方块角色在四块地图之间走
- 可以 **砍树、钓鱼**，把东西装进 **格子背包**
- 回村子 **卖货、扩包、进仓库、存档**
- 森林里有只史莱姆会撞你；死了会回到存档点
- 关掉网页再开，**进度还在**（本地存档）；离开前在采集的话，还会 **离线补一点进度**

它 **不是** 用 Python 写的。  
你熟悉的 Python 更像「脚本/后端」；这个游戏跑在 **网页浏览器** 里，所以用的是 **TypeScript（很像带类型的 JavaScript）**。

可以把关系想成：

| 你熟悉的 | 这个项目里类似的 |
|---------|------------------|
| `.py` 文件 | `.ts`（TypeScript）文件 |
| `print()` | 在网页上画图 / 改界面文字 |
| `dict` / `list` | 对象 `{}` / 数组 `[]` |
| `class` | 也有 `class`（玩家、背包、游戏主循环） |
| `if __name__ == "__main__"` 启动 | `main.ts` 创建游戏并 `start()` |

---

## 1. 游戏想做成什么样？（设计想法）

设计文档在这些文件里（**还不是代码，是想法笔记**）：

| 文件 | 干什么用 |
|------|----------|
| `构思.txt` | 长期方向：技能养成 + 自己跑图、战采对半、离线补进度、不要沙盒建城…… |
| `游戏介绍.txt` | 用更口语的方式介绍「这是个什么游戏」 |
| `第一版架构_v0.1.txt` | **只做开局这一小段** 时，系统怎么拆、四张图怎么接 |

一句话气质：

> **梅沃尔式「技能随便练、资源互喂」** ＋ **你亲手操控角色去跑图**，不是纯挂机点一下。

第一版故意做得很小，只验证「好玩的骨架能不能跑通」：

```
村落 ── 草地 ── 河边 ── 森林
```

| 地图 | 干什么 |
|------|--------|
| 村落 | 商店、仓库、存档点、出生点 |
| 草地 | 过渡，有树可砍 |
| 河边 | 钓鱼点 |
| 森林 | 更多树 + 史莱姆（碰到掉血） |

以后要加矿区、锻造、传送、无缝大地图……都可以往上叠，不必推倒重来。

---

## 2. 整个文件夹里都有什么？

```
D:\Game\
├── 待命名\                 ← ★ 游戏主工程在这里
│   ├── 构思.txt / 游戏介绍.txt / 第一版架构_v0.1.txt
│   └── beta0.01\               ← 真正能跑的第一版代码
│       ├── package.json    ← 依赖与启动命令
│       ├── index.html      ← 网页入口（一块画布 + 状态栏）
│       └── src\            ← 全部游戏逻辑
│
├── LibreSprite\            ← 像素画工具（画角色/道具用，类似 Aseprite 开源版）
├── Sprite-0001.ase / .png  ← 试画的精灵图
├── map.tmx                 ← Tiled 地图编辑器相关文件（探索用）
├── hexmini.tsx             ← Tiled 图块集引用（探索用）
└── 未命名.tiled-session    ← Tiled 编辑器会话
```

**美术（v0.1 已接入像素图）：**  
- 游戏加载 `beta0.01/public/assets/*.png`（16×16 为主）  
- 源工程在 `art/`：可用 Python 脚本重生成，也可用 **LibreSprite** 打开 `art/ase/*.ase` 手改  
- 缺图时仍会回退到色块，不会白屏

---

## 3. 用了哪些「工具」？—— 按环节拆开讲

### 3.1 写代码与跑起来：Vite + TypeScript

| 工具 | 像什么 | 为什么用 |
|------|--------|----------|
| **TypeScript** | 带「类型检查」的 JavaScript | 游戏状态多（钱、血、背包、地图 id……），类型能减少低级错误；比裸 JS 更像「写清楚说明书」 |
| **Vite** | 本地小服务器 + 打包器 | `npm run dev` 一开，浏览器改代码就热更新，开发很快；最后也能 `build` 成静态网页 |
| **浏览器** | 真正的「游戏机」 | 平台定的是「先网页单机」——别人双击/打开链接就能玩，不用装 Unity/Steam |

`package.json` 里依赖非常克制，**几乎没有第三方游戏引擎**：

```json
"devDependencies": {
  "typescript": "...",
  "vite": "..."
}
```

**为什么不用 Unity / Godot / Phaser？**

- 第一版目标是「垂直切片」：证明循环好玩，而不是做 3A
- 直接用 **Canvas 2D 自己画**，代码全在自己手里，结构清晰，方便按架构文档拆模块
- 对这个体量，引擎反而可能更重、学习成本更高
- 以后若需要动画/物理再评估；现在手写更轻

### 3.2 画面怎么显示：HTML + Canvas 2D + CSS

打开 `beta0.01/index.html`，核心只有这些：

```html
<div id="app">
  <canvas id="game"></canvas>   <!-- 游戏画面：在这块「画板」上每帧重画 -->
  <div id="hud"></div>          <!-- 文字状态栏：金币、昼夜、提示等 -->
</div>
<script type="module" src="/src/main.ts"></script>
```

| 技术 | 干什么 | 类比 |
|------|--------|------|
| **Canvas 2D** | 画地图底色、角色、树、鱼点、怪、进度条 | 像每秒擦黑板再画一遍 |
| **DOM + CSS** | 背包/商店/仓库面板、HUD 文字 | 像网页按钮和列表，适合做 UI |
| **requestAnimationFrame** | 驱动「游戏循环」 | 浏览器说「下一帧可以画了」，我们就更新逻辑再画 |

**为什么画面用 Canvas、菜单用网页元素？**

- 游戏世界（移动、碰撞、采集进度）适合每帧重绘 → Canvas
- 商店按钮、格子列表适合点选 → 普通 HTML 更省事、也好读

### 3.3 存档：浏览器 localStorage

| 技术 | 干什么 | 为什么 |
|------|--------|--------|
| **localStorage** | 把进度写成一段 JSON 字符串存在本机浏览器里 | 零后端、零账号，符合「先网页单机」 |
| 键名 | `game-v0.1-save` | 和版本绑定，换大版本可以作废旧档 |

类比 Python：

```python
# 伪代码感觉
import json
# 浏览器里类似：
localStorage["game-v0.1-save"] = json.dumps(存档字典)
存档字典 = json.loads(localStorage["game-v0.1-save"])
```

关掉标签页时、定时自动存时、买卖成功时，都会写档。  
**没有服务器**，所以清浏览器数据、换电脑会丢档——以后可以加「导出文件 / 云存档」，结构已经按 JSON 预留了。

### 3.4 美术与地图工具（目前偏「预备」）

| 工具 | 干什么 | 在 v0.1 里的角色 |
|------|--------|------------------|
| **LibreSprite** | 画像素角色、道具、动画 | 有试画素材，**尚未接入游戏** |
| **Tiled**（`map.tmx` 等） | 可视化摆地图、碰撞层 | 探索过工具链，**当前四图是代码注册表生成的** |

**为什么先代码摆图、后美术？**

1. 先验证玩法循环（采→卖→扩包→再出门）值不值得做精
2. 色块原型改起来快：改个颜色、加个出口不用等画师
3. 架构里地图已是「块（Chunk）+ 邻接表」，以后换成 Tiled 导出的数据，不必推翻移动/切屏逻辑

---

## 4. 代码怎么分层？—— 像搭乐高，不堆成一坨

架构文档把游戏想成「从下到上」几层。文件夹和它一一对应：

```
src/
├── main.ts              入口：找到画布，new Game，start
├── core/                核心：配置、输入、主循环
├── world/               世界：地图块、切屏、画地面/设施/互动物
├── entities/            实体：玩家、树/鱼点、设施、史莱姆
├── systems/             玩法系统：采集、背包、商店、技能、昼夜、离线……
├── data/                纯数据：物品表、技能表、各图上有什么
├── ui/                  面板：背包 / 商店 / 仓库
├── save/                存读档
└── style.css            外观样式
```

### 用生活类比

| 文件夹 | 像什么 |
|--------|--------|
| `core` | 导演 + 节拍器：每帧喊「更新！画！」 |
| `world` | 舞台布景：现在在哪一幕、走到边缘换幕 |
| `entities` | 演员：人、树、鱼点、怪 |
| `systems` | 规则书：怎么采、怎么卖、怎么扣血 |
| `data` | 配置表：木头卖几金、树多久再生 |
| `ui` | 菜单界面 |
| `save` | 存档U盘 |

**为什么要分这么细？**

- 加「新地图」主要改 `data` + `registry`，少动主循环
- 加「新技能」主要加数据 + 一种互动类型
- 避免出现一个 3000 行的「上帝文件」——改一处崩一片

原则一句话：**系统尽量不互相硬绑；能当数据的别写死在逻辑里。**

---

## 5. 程序是怎么「跑起来」的？（游戏循环）

### 5.1 启动链路

```
打开网页
  → 加载 index.html
  → 执行 main.ts
  → new Game(画布, HUD, 页面容器)
  → 尝试 loadSave()（有档读档 + 离线结算）
  → game.start()
  → 每一帧 requestAnimationFrame → frame()
```

### 5.2 每一帧在干什么？（最重要的概念）

伪代码（你用 Python 也能读懂）：

```text
每一帧:
  1. 算 dt = 距上一帧过了多少秒
  2. 读键盘（WASD / E / B …）
  3. 更新逻辑
       - 玩家移动
       - 是否走出地图边界 → 切屏
       - 是否在采集 / 开店 / 开仓
       - 史莱姆追人、扣血、死亡回档点
       - 昼夜往前走一点点
       - 到时间就自动存档
  4. 清空画布，重新画：
       - 地面颜色
       - 树、鱼点、设施
       - 玩家、史莱姆
       - 采集进度条、受击闪红等
  5. 刷新 HUD 文字
```

`dt`（delta time）很重要：  
不管电脑快还是慢，**移动距离 = 速度 × 时间**，手感才一致。  
Python 里写小游戏也常用同样思路。

### 5.3 核心文件速查

| 文件 | 白话职责 |
|------|----------|
| `core/config.ts` | 全局数字：格子 16px、背包几格、史莱姆秒伤、存档键名、键位…… |
| `core/input.ts` | 监听键盘，变成「这一帧要往哪走、有没有按互动」 |
| `core/game.ts` | **总指挥**：把所有系统串在一起 |
| `entities/player.ts` | 玩家坐标 + 朝向 |
| `world/world.ts` | 当前在哪张图；出界就换图 |
| `world/registry.ts` | 四张图的邻接关系（左邻右舍） |
| `world/transition.ts` | 切屏时人从哪边进来 |
| `systems/interaction.ts` | 靠近目标 + 按住 E → 蓄力 → 掉落 + 经验 |
| `systems/inventory.ts` | 格子背包/仓库（堆叠、满格判断） |
| `systems/shop.ts` | 卖资源、花钱扩包/扩仓 |
| `systems/skills.ts` | 砍树/钓鱼等级与经验 |
| `systems/timeOfDay.ts` | 昼夜进度（氛围滤镜） |
| `systems/offline.ts` | 离线时长 → 补几次采集（有上限） |
| `systems/wallet.ts` | 金币 |
| `save/saveGame.ts` | JSON ↔ localStorage |
| `ui/*Panel.ts` | 三个面板的开关与按钮回调 |

---

## 6. 几个关键玩法，是怎么用代码实现的？

### 6.1 四图切屏（不是无缝大世界）

邻接写在 `world/registry.ts` 里，类似：

```text
村落.right = 草地
草地.left = 村落，草地.right = 河边
河边.left = 草地，河边.right = 森林
森林.left = 河边
```

玩家移动后，`World.resolvePlayerBounds` 会问：

1. 人是不是走出了当前块边缘？
2. 那边有没有邻居？  
   - 有 → 换成邻居块，人放到对侧入口  
   - 没有 → 夹回边界（撞墙）

**为什么先切屏、不无缝？**

- 实现简单，一版就能「感觉像能走很远」
- 数据模型仍是「块 + 坐标」；以后无缝 = 同时加载邻块 + 镜头跟着走，**模型不用扔**

### 6.2 采集（砍树 / 钓鱼）

数据层规定「这张图上有哪些可互动物」：

- `data/mapInteractables.ts`：哪棵树在哪、鱼点在哪
- `entities/interactable.ts`：类型（tree / fish）、采集时间、掉什么、多久再生

逻辑在 `systems/interaction.ts`：

1. 找你面朝方向、够得着的目标
2. **按住** 互动键（E / 空格）→ 进度条涨
3. 满了 → 物品进背包、技能加经验、资源进入冷却（过一阵再长出来）
4. 背包满 → 提示，不白砍

**为什么是「按住」不是「点一下就出货」？**

- 有一点操作在场感（你在干活，不是点菜单）
- 又不会做成音游式难度，符合设计里「操作保持简单」

### 6.3 背包 vs 仓库

两者 **都是格子库存**，代码上复用 `Inventory` 类，只是：

- 背包：出门随身带，格子少
- 仓库：村里存取，格子多一点；各村镇共用同一份数据（v0.1 只有一个村，但结构已是「一份仓库」）

商店可以：

- 卖木头 / 生鱼换钱
- 花钱给背包、仓库 **加格子**（价格递增）

### 6.4 商店 / 仓库 UI

`ui/shopPanel.ts`、`ui/warehousePanel.ts` 用网页元素做按钮。  
**面板自己不算账**，只调用 `Game` 里接好的回调，例如「点卖木头 → `shop.sellAll(...)`」。

这样 UI 和规则分离：以后换皮肤、换按钮布局，不会搞乱卖价逻辑。

### 6.5 昼夜

`timeOfDay` 用真实时间推进「一天进度」（配置里大约 120 秒一轮）。  
画面上叠一层明暗/色调，**目前主要是氛围**；以后可以再挂「夜里怪更凶 / 鱼更好钓」等规则。

### 6.6 史莱姆与死亡

- 森林里有一只 `Slime`，会靠近玩家
- 碰到持续掉血（配置：`slimeDps`）
- 血 ≤ 0 → 传送回 **最近激活的存档点**（村里对存档点按 E 可回满血并设重生点）

死亡偏温和：符合「经营探索」而不是硬核惩罚。

### 6.7 本地存档 + 离线补进度

存档里大致包括：

- 金币、血量、当前地图与坐标、朝向
- 背包、仓库、技能、商店扩容等级
- 存档点位置、昼夜进度
- 互动物状态（树是不是在冷却）
- 上次离开时的墙钟时间 `wallMs`
- 离开前最后一次采集类型（砍树还是钓鱼）

再打开时：

```text
elapsed = 现在 - wallMs
若离开前在采某种东西 → 按时间折算几次采集（有上限，不能挂机毕业）
```

上限写在 `config.ts`：例如最多折算半小时、最多补 25 次等。  
**原则：离线是减焦虑，不是主玩法。**

---

## 7. 数据驱动：为什么有 `data/` 文件夹？

举例，`data/items.ts`：

```ts
// 物品只描述「是什么」，不写「怎么采」
wood: { name: "木头", stackMax: 99, sellPrice: 2, color: "..." }
raw_fish: { name: "生鱼", stackMax: 99, sellPrice: 5, color: "..." }
```

好处：

- 改卖价、改堆叠，不用翻遍采集代码
- 以后加「矿石」= 加一条物品 + 地图上放矿点 + 一种互动类型
- 和架构文档说的一致：**优先加数据，少改核心**

技能表、各图设施/互动物同理。

---

## 8. 从 Python 学生视角：你已经懂的概念对照

| 概念 | 在本项目中 |
|------|------------|
| 变量 / 对象 | 玩家有 `x, y, facing`；钱包有 `gold` |
| 列表 / 字典 | 背包是格子数组；地图注册表是 id → 块信息 |
| 类与方法 | `Player.update()`、`Inventory.addStack()` |
| 模块 import | `import { Game } from "./core/game.ts"` |
| 主循环 | `requestAnimationFrame` 代替 `while True` |
| 读配置 | 大家读 `CONFIG`，少写魔法数字 |
| 序列化 | `JSON.stringify` / `JSON.parse` ↔ localStorage |
| 类型注解 | TypeScript 的 `type` / 接口（比 Python 的类型提示更「强制」一点） |

你 **不需要** 先学完 React/Vue 才能懂这个项目：  
它几乎是「一个 HTML 页 + 一堆 TS 模块 + 一块 Canvas」。

---

## 9. 你怎么自己跑起来？

在终端里：

```bash
cd 待命名/beta0.01
npm install
npm run dev
```

浏览器打开终端里提示的本地地址（一般是 `http://localhost:5173` 之类）。

| 命令 | 作用 |
|------|------|
| `npm run dev` | 开发模式，改代码热更新 |
| `npm run build` | 编译检查 + 打出 `dist/` 静态网页 |
| `npm run preview` | 预览打包结果 |

### 操作一览

| 键 | 作用 |
|----|------|
| WASD / 方向键 | 移动 / 走到边缘切屏 |
| E / 空格 | 设施点一下；采集要 **按住** |
| B / I | 背包 |
| Esc | 关面板 |

建议体验：出门采 → 回村卖/扩包/存仓 → 存档点回血 → 森林试毒史莱姆 → 关页再开看读档与离线。

---

## 10. 「工具选型」总表（复习用）

| 环节 | 用了什么 | 为什么 |
|------|----------|--------|
| 语言 | TypeScript | 网页原生生态 + 类型，适合中等复杂度游戏状态 |
| 工程 | Vite | 轻、快、适合纯前端单页 |
| 渲染 | Canvas 2D | 2D 俯视足够；无重型引擎依赖 |
| UI 面板 | DOM + CSS | 按钮列表比在 Canvas 里手写点击好做 |
| 主循环 | requestAnimationFrame + dt | 浏览器标准做法，帧率稳定 |
| 存档 | localStorage + JSON | 单机零后端 |
| 架构 | 分 core/world/entities/systems/data/ui/save | 可扩展，对齐设计文档 |
| 地图 v0.1 | 代码注册四 Chunk | 快速验证切屏与循环 |
| 美术 v0.1 | 代码色块占位 | 先玩法后皮囊 |
| 预备工具 | LibreSprite、Tiled | 以后换真像素/真地图 |
| 不用的 | Unity/Godot/服务器/账号 | 超出 v0.1「开局可玩」范围 |

---

## 11. 当前做到哪了？还没做什么？

### v0.1 已做到（骨架已通）

- [x] 工程骨架 + 游戏循环  
- [x] WASD + 四图切屏  
- [x] 砍树 / 钓鱼 + 格子背包 + 技能经验  
- [x] 商店卖货、扩包/扩仓  
- [x] 互通仓库（结构上已是共用一份）  
- [x] 昼夜氛围  
- [x] 存档点 + 死亡回点 + 森林史莱姆  
- [x] 本地存档 + 简版离线补进度  

### 明确不做进 v0.1（以后再说）

- 全技能线、烹饪锻造、完整战斗三系  
- 无缝大地图、建城、沙盒改地形  
- 云存档账号、主线剧情、复杂魔法  
- 真像素角色动画全面替换色块（工具已备，接入是后续工作）

---

## 12. 如果你想「读代码」，推荐顺序

别从上到下硬啃 `game.ts` 全文。建议：

1. `构思.txt`（知道要做什么）→ `第一版架构_v0.1.txt`（知道怎么拆）  
2. `src/main.ts`（入口只有几行）  
3. `src/core/config.ts`（所有「魔法数字」在这）  
4. `src/entities/player.ts` + `src/core/input.ts`（移动怎么来的）  
5. `src/world/registry.ts` + `transition.ts` + `world.ts`（切屏）  
6. `src/systems/interaction.ts` + `inventory.ts`（采集进包）  
7. `src/save/saveGame.ts` + `systems/offline.ts`（存档与离线）  
8. 最后再看 `core/game.ts`（总装车间）

读的时候问自己三句：

- 这一帧 **输入** 是什么？  
- **状态**（位置、背包、血）怎么变？  
- **画面/UI** 怎么反映状态？  

游戏编程大体就是在重复这三步。

---

## 13. 总结：我是怎么「做出」这个游戏的？

用一条流水线概括：

1. **先定玩法与边界**（构思 + 架构文档）—— 只做开局四图，证明循环  
2. **选最轻的技术栈**（Vite + TS + Canvas）—— 网页单机、好改、好拆  
3. **按层写模块**—— 核心循环串起世界、实体、系统、数据、UI、存档  
4. **数据与逻辑分开**—— 物品/地图点位可配置，少写死  
5. **色块原型先可玩**—— 美术工具（LibreSprite/Tiled）并行准备，不挡进度  
6. **存档与离线用本地时间戳**—— 符合「挂机只是助手」  
7. **留扩展口**—— 新图、新技能、以后无缝/云存档，尽量加而不推翻  

所以你看到的不是「一个神秘黑盒游戏」，而是：

> **一份写清楚的设计** ＋ **一组分工明确的 TypeScript 模块** ＋ **浏览器每秒画很多遍的小画布**。

名字还叫「待命名」——内容方向已经比较清楚，名字可以以后用北欧/奇幻自造感再定。

---

*文档对应工程：`待命名/beta0.01`，版本 v0.1。*  
*若代码大改，以 `src/` 与 `第一版架构_v0.1.txt` 为准，本文可再更新。*
